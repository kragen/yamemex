- pop up an icon on every page with a content script
   D add a content script
   D import jQuery
   - add the icon
- when you click on the icon, open an annotation window
- send the annotations to the background page to store in SQL
- get annotations from the background page at startup and don’t show
   the icon if there are none